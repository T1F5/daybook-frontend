import Textarea from '@components/Textarea'
import styled from '@emotion/styled'

function 쓰기() {
  return (
    <Wrapper>
      <Textarea />
    </Wrapper>
  )
}

export default 쓰기

const Wrapper = styled.div`
  margin-top: 40px;
`
